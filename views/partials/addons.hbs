<h3>Selected parts of cookie</h3>
<ul>
  <li>Base:
    {{cookie.base}}
    -
    {{pricify (find-price bases cookie.base)}}
    PLN</li>
  {{#each cookie.addons}}
    <li>
      {{#unless ../noDeleteBtn}}<a
          href='/configurator/delete-addon/{{this}}'
          class='btn btn-detele'
        >Delete</a>{{/unless}}
      Addon:
      {{this}}
      -
      {{pricify (find-price @root.addons this)}}
      PLN
    </li>
  {{/each}}
</ul>

<p>
  <strong>Total of {{pricify sum}} PLN</strong>
</p>